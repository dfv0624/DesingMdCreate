{
  "name": "design-md-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@google/genai": "^2.13.0",
    "dotenv": "^17.4.2",
    "fastify": "^5.2.0",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.2"
  }
}
